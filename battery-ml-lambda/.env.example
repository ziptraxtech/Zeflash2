# ==============================================================================
# Battery ML API - Environment Configuration
# ==============================================================================
# Copy this file to .env for local development
# NEVER commit the .env file to git!
# ==============================================================================

# ==============================================================================
# AWS CREDENTIALS (Local Development Only)
# ==============================================================================
# NOTE: For ECS/Production, these are NOT needed! The ECS task uses IAM roles.
# Only set these for local development/testing
# Get these from: aws configure OR AWS IAM Console

# AWS_ACCESS_KEY_ID=your_local_dev_access_key_here
# AWS_SECRET_ACCESS_KEY=your_local_dev_secret_key_here
AWS_DEFAULT_REGION=us-east-1

# ==============================================================================
# S3 STORAGE CONFIGURATION
# ==============================================================================
# Where to store generated ML reports and visualizations

S3_BUCKET=battery-ml-results-test
S3_PREFIX=battery-reports/

# Optional: Store reports locally instead of S3 (for offline development)
# LOCAL_REPORTS_DIR=./reports

# ==============================================================================
# CMS API CONFIGURATION
# ==============================================================================
# Endpoints for fetching battery data from CMS

# Token endpoint to get authentication
TOKEN_ENDPOINT=https://cms.charjkaro.in/admin/api/v1/zipbolt/token

# API base URL for fetching charger time-lapsed data
API_BASE_URL=https://cms.charjkaro.in/commands/secure/api/v1/get/charger/time_lapsed

# Optional: If you have a Basic Auth token, set it here
# CMS_BASIC_TOKEN=your_basic_auth_token_here

# ==============================================================================
# DYNAMODB CONFIGURATION (if using DynamoDB instead of API)
# ==============================================================================
# These are used by inference.py if you want to fetch data from DynamoDB

# BATTERY_TABLE_NAME=BatteryChargingData
# DEVICE_KEY_NAME=device_id

# ==============================================================================
# MODEL CONFIGURATION
# ==============================================================================
# Directory where ML models are stored

MODEL_DIR=models

# ==============================================================================
# SERVER CONFIGURATION (Optional)
# ==============================================================================
# FastAPI server settings

# PORT=8000
# HOST=0.0.0.0

# ==============================================================================
# VERCEL CONFIGURATION (Frontend Proxy)
# ==============================================================================
# This is set in Vercel dashboard, not in this file
# Used by api/ml-proxy to forward requests to AWS ECS backend

# VITE_ML_BACKEND_URL=http://battery-ml-alb-1652817744.us-east-1.elb.amazonaws.com

# ==============================================================================
# SECURITY NOTES
# ==============================================================================
# 1. NEVER commit this file with real credentials to GitHub!
# 2. For production (ECS), use IAM roles - NO credentials needed
# 3. For local dev, use AWS CLI credentials or set AWS_* env vars
# 4. For Vercel, set VITE_ML_BACKEND_URL in Vercel dashboard
# 5. Keep .env in .gitignore at all times
# 6. Rotate credentials regularly
# 7. Use AWS Secrets Manager for production secrets
# ==============================================================================
