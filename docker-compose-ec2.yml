version: '3.8'

services:
  zeflash-backend:
    image: 070872471952.dkr.ecr.us-east-1.amazonaws.com/battery-ml:ec2-backend
    container_name: zeflash-backend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
      - ML_BACKEND_URL=${ML_BACKEND_URL:-http://battery-ml-alb-1652817744.us-east-1.elb.amazonaws.com:8000}
      - BACKEND_API_URL=${BACKEND_API_URL:-http://3.90.162.23:3000}
      - S3_BUCKET=${S3_BUCKET}
      - AWS_REGION=${AWS_REGION}
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      - VITE_API_BASE=https://zeflash.vercel.app
      - DATABASE_URL=${DATABASE_URL}
      - DIRECT_URL=${DIRECT_URL}
      - CLERK_SECRET_KEY=${CLERK_SECRET_KEY}
      - RAZORPAY_KEY_ID=${RAZORPAY_KEY_ID}
      - RAZORPAY_KEY_SECRET=${RAZORPAY_KEY_SECRET}
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  backend-data:
